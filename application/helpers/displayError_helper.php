<?php

/**
 * Description of displayError_helper
 *
 * @author admin
 */

if(!function_exists('create_error_data')) {
    function create_error_data($status, $color, $msg) {
        
        $allowed_status = ['error', 'success'];
        
        if($status == 'success') {
            
        }elseif($status == 'error') {
            
        }
        
        /*return [
            'msg' =>,
            'icone' => ,
            'color' =>
        ];*/
    }
}